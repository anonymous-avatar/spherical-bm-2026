# Fig. 13 — SBM vs unconstrained Gaussian comparison

Analytical. No simulation data.

```bash
julia --project=../_julia_env ../_julia_env/setup.jl    # one-time
julia -t auto fig_sbm_vs_gaussian.jl
```
