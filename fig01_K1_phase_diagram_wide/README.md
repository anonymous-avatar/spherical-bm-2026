# Fig. 1 — K=1 phase diagram and double descent

Analytical (RMT closed form). No simulation data.

```bash
julia --project=../_julia_env ../_julia_env/setup.jl    # one-time
julia fig2_K=1_phase_diagram_wide.jl
```
